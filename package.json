{
	"name": "root",
	"private": true,
	"scripts": {
		"installAll": "npm i && npm run lerna && npm run buildCore",
		"lerna": "npx lerna bootstrap --hoist",
		"clean": "npx ts-node cleanup.ts",
		"cleanNodemodules": "rmdir /Q /S \"./node_modules\"",
		"buildCore": "npm run buildDev --prefix ./core/core-common && npm run buildDev --prefix ./core/core-backend && npm run buildDev --prefix ./core/core-frontend"
	},
	"devDependencies": {
		"@types/node": "14.0.13",
		"lerna": "^3.22.1",
		"ts-node": "^8.10.2",
		"typescript": "^3.9.5"
	}
}